# Picoservice-rs

Simple application - exposes four endpoints on port 3000

- / :: Homepage "hello world"
- /slow :: Response delayed by a second
- /health :: Response of version, app name & status
- /metrics :: Prometheus metrics endpoint
